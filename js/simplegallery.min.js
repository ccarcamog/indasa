function newCarousel (className, miliseconds) {
	$(className).carousel({
		interval: miliseconds
	});
}
$(document).ready(function () {
	$('.thumb').on('click', function () {
		id = $(this).data('id');
		carousel = $(this).data('galeria')
			
		$(carousel).carousel(id);

	});



});